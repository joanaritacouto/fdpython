def loadcsvfile(nomefich):	
	lst=[]
	fin= open(nomefich)

	for line in fin:
		t= tuple(line.split(','))
		lst.append(t)
	
	return lst   #lista de túpulos, e cada túpulo tem a forma (nome, data, ...)

def aliiaA(t,l,c):
	l=0
	c=6
	d={}		
	while len(line)!=0:
		if
		main (l,c)
		l+=1

def main(l,c):
		tabela = lerficheiro('stocks.cvs')

	return tabela[l][c]

main()

#alínea a

